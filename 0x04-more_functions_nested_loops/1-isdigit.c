#include "main.h"

/**
 * _isdigit - function to check if a variable is a base 10 digit 0-9
 *
 * @c: variable to be checked
 *
 * Return: 1 if digit, otherwise 0
 */

int _isdigit(int c)
{
	return(c >= '0' && c <= '9');
}
